import 'package:flutter/material.dart';

class FrsPage extends StatelessWidget {
  const FrsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('FRS page')));
  }
}
