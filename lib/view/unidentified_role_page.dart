import 'package:flutter/material.dart';

class UnIdentifiedRole extends StatelessWidget {
  const UnIdentifiedRole({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Role tidak di ketahui')));
  }
}
